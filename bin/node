#!/usr/bin/env node

const config = require('../config')
const Node = require('../lib/node')
// const Storage = require('../lib/storage')

// config.storage = Storage(config.storage.location)

const node = Node(config)

node.connect()
